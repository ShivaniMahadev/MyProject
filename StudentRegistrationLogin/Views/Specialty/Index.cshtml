@model IEnumerable<StudentRegistrationLogin.Models.Specialty>
@Html.Partial("_AdminLayoutHeader")
@{
    ViewBag.Title = "Index";
    Layout = null;
}

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <p>
                            <button class="btn btn-success" style="color:white">
                                @Html.ActionLink("Create New", "Create", null, new { @style = "color:white" })
                            </button>
                        </p>
                        <h3 class="card-title">Manage Specialty</h3>
                    </div>
                    <div class="card-body">
                        <table class="table">
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.SpecialtyName)
                                </th>
                                <th></th>
                            </tr>

                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.SpecialtyName)
                                    </td>
                                    <td>
                                        <button class="btn btn-info" style="color:white">@Html.ActionLink("Edit", "Edit", null, new { id = item.SpecialtyID }, new { @style = "color:white" })</button>
                                        <button class="btn btn-danger" style="color:white">@Html.ActionLink("Delete", "Delete", new { id = item.SpecialtyID }, new { @style = "color:white" })</button>
                                        @*@Html.ActionLink("Edit", "Edit", new { id = item.SpecialtyID }) |
@Html.ActionLink("Details", "Details", new { id = item.SpecialtyID }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.SpecialtyID })*@
                                    </td>
                                </tr>
                            }

                        </table>
                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
                <!-- /.card -->
            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
</section>
@Html.Partial("_AdminLayoutFooter")
